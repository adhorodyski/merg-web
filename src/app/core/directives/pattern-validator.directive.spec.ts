import { PatternValidatorDirective } from './pattern-validator.directive';

describe('PatternValidatorDirective', () => {
    it('should create an instance', () => {
        const directive = new PatternValidatorDirective();
        expect(directive).toBeTruthy();
    });
});
